// Package streams provides a set of Source and Sink semantics to plug together object streams.  Streams are closer to
// NodeJS object stream semantics as opposed to bufio byte level semantics.  Although nothing stops one from putting
// together a byte stream.
package streams
