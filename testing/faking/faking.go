// Package faking provides utilities for generating unique fake data for testing.
//
// The primary type is UniqueUniverse, which ensures each generated value is
// distinct from all previous values. NewUniqueWords returns a ready-to-use
// universe for random words, and DistinctWords is a convenience function for
// generating a batch of unique words in one call.
package faking
